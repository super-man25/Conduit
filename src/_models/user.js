// @flow

export type EDUser = {
  clientId: number,
  createdAt: Date,
  email: string,
  firstName: string,
  id: number,
  isAdmin: boolean,
  lastName: string,
  modifiedAt: Date,
  phoneNumber: string,
};
