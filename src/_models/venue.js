// @flow

export type EDVenue = {
  id: number,
  name: string,
  svgUrl: string,
};

export type EDVenuePriceScale = {
  id: number,
  name: string,
  venueId: number,
  integrationId: number,
};

export type EDVenueSVGMapping = {};
