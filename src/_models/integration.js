// @flow

export type EDIntegration = {
  appId: string,
  createdAt: Date,
  id: number,
  isActive: boolean,
  isPrimary: boolean,
  modifiedAt: Date,
  name: string,
  version: string,
  logoUrl: string,
};
