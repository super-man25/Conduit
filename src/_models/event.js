// @flow

export type EDEvent = {
  clientId: number,
  createdAt: Date,
  id: number,
  integrationId: number,
  modifiedAt: Date,
  name: string,
  seasonId: number,
  timestamp: Date,
  venueId: number,
  isBroadcast: boolean
};
